# Cadence — Passenger / Socket.IO on DirectAdmin
# One Node process + sticky sessions (required without a Redis Socket.IO adapter).
PassengerMaxInstances 1
PassengerStickySessions on
# Recycle less often — frequent restarts drop active Socket.IO sessions.
PassengerMaxPoolSize 500
